.idea
cmake-*
eigen

build/

# ignore dens but only in the data directory so test data is seen
data/*.dens
# ignore int but only in the data directory so test data is seen
data/*.int
*.csv
# ignore the numerous files that are created from a resolution analysis
exercises/ex9/a?/res_ana_*

*.pyc
